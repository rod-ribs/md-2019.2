(in-package :snark-user)

(initialize)
(use-hyperresolution t)

;; https://www.csie.ntu.edu.tw/~lyuu/complexity/2011/20111018.pdf

(defparameter *graph* '((1 (2 4 9))
			(2 (1 3 4 6))
			(3 (2 5 7 8 9))
			(4 (1 2 5))
			(5 (3 4 6 9))
			(6 (2 5 8 9))
			(7 (3 8))
			(8 (3 6 7 9))
			(9 (1 3 5 6 8))))


;; axiom 5
;; (let ((V (list 1 2 3 4 5 6 7 8 9)))
;;   (loop for k in '(1 2 3 4 5 6 7 8)
;; 	append (loop for i in V
;; 		     append (loop for j in V
;; 				  when (and (not (equal i j)) (not (find j (cadr (assoc i *graph*)))))
;; 				    collect `(or
;; 					      (not ,(format nil "x~a~a" k i))
;; 					      (not ,(format nil "x~a~a" (+ 1 k) j)))))))
(assert '(and
	  (OR (NOT x11) (NOT x23)) (OR (NOT x11) (NOT x25))
	  (OR (NOT x11) (NOT x26)) (OR (NOT x11) (NOT x27))
	  (OR (NOT x11) (NOT x28)) (OR (NOT x12) (NOT x25))
	  (OR (NOT x12) (NOT x27)) (OR (NOT x12) (NOT x28))
	  (OR (NOT x12) (NOT x29)) (OR (NOT x13) (NOT x21))
	  (OR (NOT x13) (NOT x24)) (OR (NOT x13) (NOT x26))
	  (OR (NOT x14) (NOT x23)) (OR (NOT x14) (NOT x26))
	  (OR (NOT x14) (NOT x27)) (OR (NOT x14) (NOT x28))
	  (OR (NOT x14) (NOT x29)) (OR (NOT x15) (NOT x21))
	  (OR (NOT x15) (NOT x22)) (OR (NOT x15) (NOT x27))
	  (OR (NOT x15) (NOT x28)) (OR (NOT x16) (NOT x21))
	  (OR (NOT x16) (NOT x23)) (OR (NOT x16) (NOT x24))
	  (OR (NOT x16) (NOT x27)) (OR (NOT x17) (NOT x21))
	  (OR (NOT x17) (NOT x22)) (OR (NOT x17) (NOT x24))
	  (OR (NOT x17) (NOT x25)) (OR (NOT x17) (NOT x26))
	  (OR (NOT x17) (NOT x29)) (OR (NOT x18) (NOT x21))
	  (OR (NOT x18) (NOT x22)) (OR (NOT x18) (NOT x24))
	  (OR (NOT x18) (NOT x25)) (OR (NOT x19) (NOT x22))
	  (OR (NOT x19) (NOT x24)) (OR (NOT x19) (NOT x27))
	  (OR (NOT x21) (NOT x33)) (OR (NOT x21) (NOT x35))
	  (OR (NOT x21) (NOT x36)) (OR (NOT x21) (NOT x37))
	  (OR (NOT x21) (NOT x38)) (OR (NOT x22) (NOT x35))
	  (OR (NOT x22) (NOT x37)) (OR (NOT x22) (NOT x38))
	  (OR (NOT x22) (NOT x39)) (OR (NOT x23) (NOT x31))
	  (OR (NOT x23) (NOT x34)) (OR (NOT x23) (NOT x36))
	  (OR (NOT x24) (NOT x33)) (OR (NOT x24) (NOT x36))
	  (OR (NOT x24) (NOT x37)) (OR (NOT x24) (NOT x38))
	  (OR (NOT x24) (NOT x39)) (OR (NOT x25) (NOT x31))
	  (OR (NOT x25) (NOT x32)) (OR (NOT x25) (NOT x37))
	  (OR (NOT x25) (NOT x38)) (OR (NOT x26) (NOT x31))
	  (OR (NOT x26) (NOT x33)) (OR (NOT x26) (NOT x34))
	  (OR (NOT x26) (NOT x37)) (OR (NOT x27) (NOT x31))
	  (OR (NOT x27) (NOT x32)) (OR (NOT x27) (NOT x34))
	  (OR (NOT x27) (NOT x35)) (OR (NOT x27) (NOT x36))
	  (OR (NOT x27) (NOT x39)) (OR (NOT x28) (NOT x31))
	  (OR (NOT x28) (NOT x32)) (OR (NOT x28) (NOT x34))
	  (OR (NOT x28) (NOT x35)) (OR (NOT x29) (NOT x32))
	  (OR (NOT x29) (NOT x34)) (OR (NOT x29) (NOT x37))
	  (OR (NOT x31) (NOT x43)) (OR (NOT x31) (NOT x45))
	  (OR (NOT x31) (NOT x46)) (OR (NOT x31) (NOT x47))
	  (OR (NOT x31) (NOT x48)) (OR (NOT x32) (NOT x45))
	  (OR (NOT x32) (NOT x47)) (OR (NOT x32) (NOT x48))
	  (OR (NOT x32) (NOT x49)) (OR (NOT x33) (NOT x41))
	  (OR (NOT x33) (NOT x44)) (OR (NOT x33) (NOT x46))
	  (OR (NOT x34) (NOT x43)) (OR (NOT x34) (NOT x46))
	  (OR (NOT x34) (NOT x47)) (OR (NOT x34) (NOT x48))
	  (OR (NOT x34) (NOT x49)) (OR (NOT x35) (NOT x41))
	  (OR (NOT x35) (NOT x42)) (OR (NOT x35) (NOT x47))
	  (OR (NOT x35) (NOT x48)) (OR (NOT x36) (NOT x41))
	  (OR (NOT x36) (NOT x43)) (OR (NOT x36) (NOT x44))
	  (OR (NOT x36) (NOT x47)) (OR (NOT x37) (NOT x41))
	  (OR (NOT x37) (NOT x42)) (OR (NOT x37) (NOT x44))
	  (OR (NOT x37) (NOT x45)) (OR (NOT x37) (NOT x46))
	  (OR (NOT x37) (NOT x49)) (OR (NOT x38) (NOT x41))
	  (OR (NOT x38) (NOT x42)) (OR (NOT x38) (NOT x44))
	  (OR (NOT x38) (NOT x45)) (OR (NOT x39) (NOT x42))
	  (OR (NOT x39) (NOT x44)) (OR (NOT x39) (NOT x47))
	  (OR (NOT x41) (NOT x53)) (OR (NOT x41) (NOT x55))
	  (OR (NOT x41) (NOT x56)) (OR (NOT x41) (NOT x57))
	  (OR (NOT x41) (NOT x58)) (OR (NOT x42) (NOT x55))
	  (OR (NOT x42) (NOT x57)) (OR (NOT x42) (NOT x58))
	  (OR (NOT x42) (NOT x59)) (OR (NOT x43) (NOT x51))
	  (OR (NOT x43) (NOT x54)) (OR (NOT x43) (NOT x56))
	  (OR (NOT x44) (NOT x53)) (OR (NOT x44) (NOT x56))
	  (OR (NOT x44) (NOT x57)) (OR (NOT x44) (NOT x58))
	  (OR (NOT x44) (NOT x59)) (OR (NOT x45) (NOT x51))
	  (OR (NOT x45) (NOT x52)) (OR (NOT x45) (NOT x57))
	  (OR (NOT x45) (NOT x58)) (OR (NOT x46) (NOT x51))
	  (OR (NOT x46) (NOT x53)) (OR (NOT x46) (NOT x54))
	  (OR (NOT x46) (NOT x57)) (OR (NOT x47) (NOT x51))
	  (OR (NOT x47) (NOT x52)) (OR (NOT x47) (NOT x54))
	  (OR (NOT x47) (NOT x55)) (OR (NOT x47) (NOT x56))
	  (OR (NOT x47) (NOT x59)) (OR (NOT x48) (NOT x51))
	  (OR (NOT x48) (NOT x52)) (OR (NOT x48) (NOT x54))
	  (OR (NOT x48) (NOT x55)) (OR (NOT x49) (NOT x52))
	  (OR (NOT x49) (NOT x54)) (OR (NOT x49) (NOT x57))
	  (OR (NOT x51) (NOT x63)) (OR (NOT x51) (NOT x65))
	  (OR (NOT x51) (NOT x66)) (OR (NOT x51) (NOT x67))
	  (OR (NOT x51) (NOT x68)) (OR (NOT x52) (NOT x65))
	  (OR (NOT x52) (NOT x67)) (OR (NOT x52) (NOT x68))
	  (OR (NOT x52) (NOT x69)) (OR (NOT x53) (NOT x61))
	  (OR (NOT x53) (NOT x64)) (OR (NOT x53) (NOT x66))
	  (OR (NOT x54) (NOT x63)) (OR (NOT x54) (NOT x66))
	  (OR (NOT x54) (NOT x67)) (OR (NOT x54) (NOT x68))
	  (OR (NOT x54) (NOT x69)) (OR (NOT x55) (NOT x61))
	  (OR (NOT x55) (NOT x62)) (OR (NOT x55) (NOT x67))
	  (OR (NOT x55) (NOT x68)) (OR (NOT x56) (NOT x61))
	  (OR (NOT x56) (NOT x63)) (OR (NOT x56) (NOT x64))
	  (OR (NOT x56) (NOT x67)) (OR (NOT x57) (NOT x61))
	  (OR (NOT x57) (NOT x62)) (OR (NOT x57) (NOT x64))
	  (OR (NOT x57) (NOT x65)) (OR (NOT x57) (NOT x66))
	  (OR (NOT x57) (NOT x69)) (OR (NOT x58) (NOT x61))
	  (OR (NOT x58) (NOT x62)) (OR (NOT x58) (NOT x64))
	  (OR (NOT x58) (NOT x65)) (OR (NOT x59) (NOT x62))
	  (OR (NOT x59) (NOT x64)) (OR (NOT x59) (NOT x67))
	  (OR (NOT x61) (NOT x73)) (OR (NOT x61) (NOT x75))
	  (OR (NOT x61) (NOT x76)) (OR (NOT x61) (NOT x77))
	  (OR (NOT x61) (NOT x78)) (OR (NOT x62) (NOT x75))
	  (OR (NOT x62) (NOT x77)) (OR (NOT x62) (NOT x78))
	  (OR (NOT x62) (NOT x79)) (OR (NOT x63) (NOT x71))
	  (OR (NOT x63) (NOT x74)) (OR (NOT x63) (NOT x76))
	  (OR (NOT x64) (NOT x73)) (OR (NOT x64) (NOT x76))
	  (OR (NOT x64) (NOT x77)) (OR (NOT x64) (NOT x78))
	  (OR (NOT x64) (NOT x79)) (OR (NOT x65) (NOT x71))
	  (OR (NOT x65) (NOT x72)) (OR (NOT x65) (NOT x77))
	  (OR (NOT x65) (NOT x78)) (OR (NOT x66) (NOT x71))
	  (OR (NOT x66) (NOT x73)) (OR (NOT x66) (NOT x74))
	  (OR (NOT x66) (NOT x77)) (OR (NOT x67) (NOT x71))
	  (OR (NOT x67) (NOT x72)) (OR (NOT x67) (NOT x74))
	  (OR (NOT x67) (NOT x75)) (OR (NOT x67) (NOT x76))
	  (OR (NOT x67) (NOT x79)) (OR (NOT x68) (NOT x71))
	  (OR (NOT x68) (NOT x72)) (OR (NOT x68) (NOT x74))
	  (OR (NOT x68) (NOT x75)) (OR (NOT x69) (NOT x72))
	  (OR (NOT x69) (NOT x74)) (OR (NOT x69) (NOT x77))
	  (OR (NOT x71) (NOT x83)) (OR (NOT x71) (NOT x85))
	  (OR (NOT x71) (NOT x86)) (OR (NOT x71) (NOT x87))
	  (OR (NOT x71) (NOT x88)) (OR (NOT x72) (NOT x85))
	  (OR (NOT x72) (NOT x87)) (OR (NOT x72) (NOT x88))
	  (OR (NOT x72) (NOT x89)) (OR (NOT x73) (NOT x81))
	  (OR (NOT x73) (NOT x84)) (OR (NOT x73) (NOT x86))
	  (OR (NOT x74) (NOT x83)) (OR (NOT x74) (NOT x86))
	  (OR (NOT x74) (NOT x87)) (OR (NOT x74) (NOT x88))
	  (OR (NOT x74) (NOT x89)) (OR (NOT x75) (NOT x81))
	  (OR (NOT x75) (NOT x82)) (OR (NOT x75) (NOT x87))
	  (OR (NOT x75) (NOT x88)) (OR (NOT x76) (NOT x81))
	  (OR (NOT x76) (NOT x83)) (OR (NOT x76) (NOT x84))
	  (OR (NOT x76) (NOT x87)) (OR (NOT x77) (NOT x81))
	  (OR (NOT x77) (NOT x82)) (OR (NOT x77) (NOT x84))
	  (OR (NOT x77) (NOT x85)) (OR (NOT x77) (NOT x86))
	  (OR (NOT x77) (NOT x89)) (OR (NOT x78) (NOT x81))
	  (OR (NOT x78) (NOT x82)) (OR (NOT x78) (NOT x84))
	  (OR (NOT x78) (NOT x85)) (OR (NOT x79) (NOT x82))
	  (OR (NOT x79) (NOT x84)) (OR (NOT x79) (NOT x87))
	  (OR (NOT x81) (NOT x93)) (OR (NOT x81) (NOT x95))
	  (OR (NOT x81) (NOT x96)) (OR (NOT x81) (NOT x97))
	  (OR (NOT x81) (NOT x98)) (OR (NOT x82) (NOT x95))
	  (OR (NOT x82) (NOT x97)) (OR (NOT x82) (NOT x98))
	  (OR (NOT x82) (NOT x99)) (OR (NOT x83) (NOT x91))
	  (OR (NOT x83) (NOT x94)) (OR (NOT x83) (NOT x96))
	  (OR (NOT x84) (NOT x93)) (OR (NOT x84) (NOT x96))
	  (OR (NOT x84) (NOT x97)) (OR (NOT x84) (NOT x98))
	  (OR (NOT x84) (NOT x99)) (OR (NOT x85) (NOT x91))
	  (OR (NOT x85) (NOT x92)) (OR (NOT x85) (NOT x97))
	  (OR (NOT x85) (NOT x98)) (OR (NOT x86) (NOT x91))
	  (OR (NOT x86) (NOT x93)) (OR (NOT x86) (NOT x94))
	  (OR (NOT x86) (NOT x97)) (OR (NOT x87) (NOT x91))
	  (OR (NOT x87) (NOT x92)) (OR (NOT x87) (NOT x94))
	  (OR (NOT x87) (NOT x95)) (OR (NOT x87) (NOT x96))
	  (OR (NOT x87) (NOT x99)) (OR (NOT x88) (NOT x91))
	  (OR (NOT x88) (NOT x92)) (OR (NOT x88) (NOT x94))
	  (OR (NOT x88) (NOT x95)) (OR (NOT x89) (NOT x92))
	  (OR (NOT x89) (NOT x94)) (OR (NOT x89) (NOT x97))))


;; axiom 1 and 3
;; (loop for j in (list 1 2 3 4 5 6 7 8 9)
;;       collect (loop for i in (list 1 2 3 4 5 6 7 8 9)
;; 		    collect (format nil "x~a~a" j i)))
(assert '(and
	  (or x11 x21 x31 x41 x51 x61 x71 x81 x91)
	  (or x12 x22 x32 x42 x52 x62 x72 x82 x92)
	  (or x13 x23 x33 x43 x53 x63 x73 x83 x93)
	  (or x14 x24 x34 x44 x54 x64 x74 x84 x94)
	  (or x15 x25 x35 x45 x55 x65 x75 x85 x95)
	  (or x16 x26 x36 x46 x56 x66 x76 x86 x96)
	  (or x17 x27 x37 x47 x57 x67 x77 x87 x97)
	  (or x18 x28 x38 x48 x58 x68 x78 x88 x98)
	  (or x19 x29 x39 x49 x59 x69 x79 x89 x99)))

(assert '(and
	  (or x11 x12 x13 x14 x15 x16 x17 x18 x19)
	  (or x21 x22 x23 x24 x25 x26 x27 x28 x29)
	  (or x31 x32 x33 x34 x35 x36 x37 x38 x39)
	  (or x41 x42 x43 x44 x45 x46 x47 x48 x49)
	  (or x51 x52 x53 x54 x55 x56 x57 x58 x59)
	  (or x61 x62 x63 x64 x65 x66 x67 x68 x69)
	  (or x71 x72 x73 x74 x75 x76 x77 x78 x79)
	  (or x81 x82 x83 x84 x85 x86 x87 x88 x89)
	  (or x91 x92 x93 x94 x95 x96 x97 x98 x99)))

;; transformacao para TPTP
;; (format t "~{fof(a,axiom,~{~a ^,& ~}).~%~}" *)

;; axiom 2
;; (let ((V (list 1 2 3 4 5 6 7 8 9)))
;;   (loop for j in V
;; 	append (loop for i in V
;; 		     append (loop for k in V
;; 				  when (not (equal k i))
;; 				    collect `(or
;; 					      (not ,(format nil "x~a~a" i j))
;; 					      (not ,(format nil "x~a~a" k j)))))))

(assert '(and
	  (OR (NOT x11) (NOT x21)) (OR (NOT x11) (NOT x31))
	  (OR (NOT x11) (NOT x41)) (OR (NOT x11) (NOT x51))
	  (OR (NOT x11) (NOT x61)) (OR (NOT x11) (NOT x71))
	  (OR (NOT x11) (NOT x81)) (OR (NOT x11) (NOT x91))
	  (OR (NOT x21) (NOT x11)) (OR (NOT x21) (NOT x31))
	  (OR (NOT x21) (NOT x41)) (OR (NOT x21) (NOT x51))
	  (OR (NOT x21) (NOT x61)) (OR (NOT x21) (NOT x71))
	  (OR (NOT x21) (NOT x81)) (OR (NOT x21) (NOT x91))
	  (OR (NOT x31) (NOT x11)) (OR (NOT x31) (NOT x21))
	  (OR (NOT x31) (NOT x41)) (OR (NOT x31) (NOT x51))
	  (OR (NOT x31) (NOT x61)) (OR (NOT x31) (NOT x71))
	  (OR (NOT x31) (NOT x81)) (OR (NOT x31) (NOT x91))
	  (OR (NOT x41) (NOT x11)) (OR (NOT x41) (NOT x21))
	  (OR (NOT x41) (NOT x31)) (OR (NOT x41) (NOT x51))
	  (OR (NOT x41) (NOT x61)) (OR (NOT x41) (NOT x71))
	  (OR (NOT x41) (NOT x81)) (OR (NOT x41) (NOT x91))
	  (OR (NOT x51) (NOT x11)) (OR (NOT x51) (NOT x21))
	  (OR (NOT x51) (NOT x31)) (OR (NOT x51) (NOT x41))
	  (OR (NOT x51) (NOT x61)) (OR (NOT x51) (NOT x71))
	  (OR (NOT x51) (NOT x81)) (OR (NOT x51) (NOT x91))
	  (OR (NOT x61) (NOT x11)) (OR (NOT x61) (NOT x21))
	  (OR (NOT x61) (NOT x31)) (OR (NOT x61) (NOT x41))
	  (OR (NOT x61) (NOT x51)) (OR (NOT x61) (NOT x71))
	  (OR (NOT x61) (NOT x81)) (OR (NOT x61) (NOT x91))
	  (OR (NOT x71) (NOT x11)) (OR (NOT x71) (NOT x21))
	  (OR (NOT x71) (NOT x31)) (OR (NOT x71) (NOT x41))
	  (OR (NOT x71) (NOT x51)) (OR (NOT x71) (NOT x61))
	  (OR (NOT x71) (NOT x81)) (OR (NOT x71) (NOT x91))
	  (OR (NOT x81) (NOT x11)) (OR (NOT x81) (NOT x21))
	  (OR (NOT x81) (NOT x31)) (OR (NOT x81) (NOT x41))
	  (OR (NOT x81) (NOT x51)) (OR (NOT x81) (NOT x61))
	  (OR (NOT x81) (NOT x71)) (OR (NOT x81) (NOT x91))
	  (OR (NOT x91) (NOT x11)) (OR (NOT x91) (NOT x21))
	  (OR (NOT x91) (NOT x31)) (OR (NOT x91) (NOT x41))
	  (OR (NOT x91) (NOT x51)) (OR (NOT x91) (NOT x61))
	  (OR (NOT x91) (NOT x71)) (OR (NOT x91) (NOT x81))
	  (OR (NOT x12) (NOT x22)) (OR (NOT x12) (NOT x32))
	  (OR (NOT x12) (NOT x42)) (OR (NOT x12) (NOT x52))
	  (OR (NOT x12) (NOT x62)) (OR (NOT x12) (NOT x72))
	  (OR (NOT x12) (NOT x82)) (OR (NOT x12) (NOT x92))
	  (OR (NOT x22) (NOT x12)) (OR (NOT x22) (NOT x32))
	  (OR (NOT x22) (NOT x42)) (OR (NOT x22) (NOT x52))
	  (OR (NOT x22) (NOT x62)) (OR (NOT x22) (NOT x72))
	  (OR (NOT x22) (NOT x82)) (OR (NOT x22) (NOT x92))
	  (OR (NOT x32) (NOT x12)) (OR (NOT x32) (NOT x22))
	  (OR (NOT x32) (NOT x42)) (OR (NOT x32) (NOT x52))
	  (OR (NOT x32) (NOT x62)) (OR (NOT x32) (NOT x72))
	  (OR (NOT x32) (NOT x82)) (OR (NOT x32) (NOT x92))
	  (OR (NOT x42) (NOT x12)) (OR (NOT x42) (NOT x22))
	  (OR (NOT x42) (NOT x32)) (OR (NOT x42) (NOT x52))
	  (OR (NOT x42) (NOT x62)) (OR (NOT x42) (NOT x72))
	  (OR (NOT x42) (NOT x82)) (OR (NOT x42) (NOT x92))
	  (OR (NOT x52) (NOT x12)) (OR (NOT x52) (NOT x22))
	  (OR (NOT x52) (NOT x32)) (OR (NOT x52) (NOT x42))
	  (OR (NOT x52) (NOT x62)) (OR (NOT x52) (NOT x72))
	  (OR (NOT x52) (NOT x82)) (OR (NOT x52) (NOT x92))
	  (OR (NOT x62) (NOT x12)) (OR (NOT x62) (NOT x22))
	  (OR (NOT x62) (NOT x32)) (OR (NOT x62) (NOT x42))
	  (OR (NOT x62) (NOT x52)) (OR (NOT x62) (NOT x72))
	  (OR (NOT x62) (NOT x82)) (OR (NOT x62) (NOT x92))
	  (OR (NOT x72) (NOT x12)) (OR (NOT x72) (NOT x22))
	  (OR (NOT x72) (NOT x32)) (OR (NOT x72) (NOT x42))
	  (OR (NOT x72) (NOT x52)) (OR (NOT x72) (NOT x62))
	  (OR (NOT x72) (NOT x82)) (OR (NOT x72) (NOT x92))
	  (OR (NOT x82) (NOT x12)) (OR (NOT x82) (NOT x22))
	  (OR (NOT x82) (NOT x32)) (OR (NOT x82) (NOT x42))
	  (OR (NOT x82) (NOT x52)) (OR (NOT x82) (NOT x62))
	  (OR (NOT x82) (NOT x72)) (OR (NOT x82) (NOT x92))
	  (OR (NOT x92) (NOT x12)) (OR (NOT x92) (NOT x22))
	  (OR (NOT x92) (NOT x32)) (OR (NOT x92) (NOT x42))
	  (OR (NOT x92) (NOT x52)) (OR (NOT x92) (NOT x62))
	  (OR (NOT x92) (NOT x72)) (OR (NOT x92) (NOT x82))
	  (OR (NOT x13) (NOT x23)) (OR (NOT x13) (NOT x33))
	  (OR (NOT x13) (NOT x43)) (OR (NOT x13) (NOT x53))
	  (OR (NOT x13) (NOT x63)) (OR (NOT x13) (NOT x73))
	  (OR (NOT x13) (NOT x83)) (OR (NOT x13) (NOT x93))
	  (OR (NOT x23) (NOT x13)) (OR (NOT x23) (NOT x33))
	  (OR (NOT x23) (NOT x43)) (OR (NOT x23) (NOT x53))
	  (OR (NOT x23) (NOT x63)) (OR (NOT x23) (NOT x73))
	  (OR (NOT x23) (NOT x83)) (OR (NOT x23) (NOT x93))
	  (OR (NOT x33) (NOT x13)) (OR (NOT x33) (NOT x23))
	  (OR (NOT x33) (NOT x43)) (OR (NOT x33) (NOT x53))
	  (OR (NOT x33) (NOT x63)) (OR (NOT x33) (NOT x73))
	  (OR (NOT x33) (NOT x83)) (OR (NOT x33) (NOT x93))
	  (OR (NOT x43) (NOT x13)) (OR (NOT x43) (NOT x23))
	  (OR (NOT x43) (NOT x33)) (OR (NOT x43) (NOT x53))
	  (OR (NOT x43) (NOT x63)) (OR (NOT x43) (NOT x73))
	  (OR (NOT x43) (NOT x83)) (OR (NOT x43) (NOT x93))
	  (OR (NOT x53) (NOT x13)) (OR (NOT x53) (NOT x23))
	  (OR (NOT x53) (NOT x33)) (OR (NOT x53) (NOT x43))
	  (OR (NOT x53) (NOT x63)) (OR (NOT x53) (NOT x73))
	  (OR (NOT x53) (NOT x83)) (OR (NOT x53) (NOT x93))
	  (OR (NOT x63) (NOT x13)) (OR (NOT x63) (NOT x23))
	  (OR (NOT x63) (NOT x33)) (OR (NOT x63) (NOT x43))
	  (OR (NOT x63) (NOT x53)) (OR (NOT x63) (NOT x73))
	  (OR (NOT x63) (NOT x83)) (OR (NOT x63) (NOT x93))
	  (OR (NOT x73) (NOT x13)) (OR (NOT x73) (NOT x23))
	  (OR (NOT x73) (NOT x33)) (OR (NOT x73) (NOT x43))
	  (OR (NOT x73) (NOT x53)) (OR (NOT x73) (NOT x63))
	  (OR (NOT x73) (NOT x83)) (OR (NOT x73) (NOT x93))
	  (OR (NOT x83) (NOT x13)) (OR (NOT x83) (NOT x23))
	  (OR (NOT x83) (NOT x33)) (OR (NOT x83) (NOT x43))
	  (OR (NOT x83) (NOT x53)) (OR (NOT x83) (NOT x63))
	  (OR (NOT x83) (NOT x73)) (OR (NOT x83) (NOT x93))
	  (OR (NOT x93) (NOT x13)) (OR (NOT x93) (NOT x23))
	  (OR (NOT x93) (NOT x33)) (OR (NOT x93) (NOT x43))
	  (OR (NOT x93) (NOT x53)) (OR (NOT x93) (NOT x63))
	  (OR (NOT x93) (NOT x73)) (OR (NOT x93) (NOT x83))
	  (OR (NOT x14) (NOT x24)) (OR (NOT x14) (NOT x34))
	  (OR (NOT x14) (NOT x44)) (OR (NOT x14) (NOT x54))
	  (OR (NOT x14) (NOT x64)) (OR (NOT x14) (NOT x74))
	  (OR (NOT x14) (NOT x84)) (OR (NOT x14) (NOT x94))
	  (OR (NOT x24) (NOT x14)) (OR (NOT x24) (NOT x34))
	  (OR (NOT x24) (NOT x44)) (OR (NOT x24) (NOT x54))
	  (OR (NOT x24) (NOT x64)) (OR (NOT x24) (NOT x74))
	  (OR (NOT x24) (NOT x84)) (OR (NOT x24) (NOT x94))
	  (OR (NOT x34) (NOT x14)) (OR (NOT x34) (NOT x24))
	  (OR (NOT x34) (NOT x44)) (OR (NOT x34) (NOT x54))
	  (OR (NOT x34) (NOT x64)) (OR (NOT x34) (NOT x74))
	  (OR (NOT x34) (NOT x84)) (OR (NOT x34) (NOT x94))
	  (OR (NOT x44) (NOT x14)) (OR (NOT x44) (NOT x24))
	  (OR (NOT x44) (NOT x34)) (OR (NOT x44) (NOT x54))
	  (OR (NOT x44) (NOT x64)) (OR (NOT x44) (NOT x74))
	  (OR (NOT x44) (NOT x84)) (OR (NOT x44) (NOT x94))
	  (OR (NOT x54) (NOT x14)) (OR (NOT x54) (NOT x24))
	  (OR (NOT x54) (NOT x34)) (OR (NOT x54) (NOT x44))
	  (OR (NOT x54) (NOT x64)) (OR (NOT x54) (NOT x74))
	  (OR (NOT x54) (NOT x84)) (OR (NOT x54) (NOT x94))
	  (OR (NOT x64) (NOT x14)) (OR (NOT x64) (NOT x24))
	  (OR (NOT x64) (NOT x34)) (OR (NOT x64) (NOT x44))
	  (OR (NOT x64) (NOT x54)) (OR (NOT x64) (NOT x74))
	  (OR (NOT x64) (NOT x84)) (OR (NOT x64) (NOT x94))
	  (OR (NOT x74) (NOT x14)) (OR (NOT x74) (NOT x24))
	  (OR (NOT x74) (NOT x34)) (OR (NOT x74) (NOT x44))
	  (OR (NOT x74) (NOT x54)) (OR (NOT x74) (NOT x64))
	  (OR (NOT x74) (NOT x84)) (OR (NOT x74) (NOT x94))
	  (OR (NOT x84) (NOT x14)) (OR (NOT x84) (NOT x24))
	  (OR (NOT x84) (NOT x34)) (OR (NOT x84) (NOT x44))
	  (OR (NOT x84) (NOT x54)) (OR (NOT x84) (NOT x64))
	  (OR (NOT x84) (NOT x74)) (OR (NOT x84) (NOT x94))
	  (OR (NOT x94) (NOT x14)) (OR (NOT x94) (NOT x24))
	  (OR (NOT x94) (NOT x34)) (OR (NOT x94) (NOT x44))
	  (OR (NOT x94) (NOT x54)) (OR (NOT x94) (NOT x64))
	  (OR (NOT x94) (NOT x74)) (OR (NOT x94) (NOT x84))
	  (OR (NOT x15) (NOT x25)) (OR (NOT x15) (NOT x35))
	  (OR (NOT x15) (NOT x45)) (OR (NOT x15) (NOT x55))
	  (OR (NOT x15) (NOT x65)) (OR (NOT x15) (NOT x75))
	  (OR (NOT x15) (NOT x85)) (OR (NOT x15) (NOT x95))
	  (OR (NOT x25) (NOT x15)) (OR (NOT x25) (NOT x35))
	  (OR (NOT x25) (NOT x45)) (OR (NOT x25) (NOT x55))
	  (OR (NOT x25) (NOT x65)) (OR (NOT x25) (NOT x75))
	  (OR (NOT x25) (NOT x85)) (OR (NOT x25) (NOT x95))
	  (OR (NOT x35) (NOT x15)) (OR (NOT x35) (NOT x25))
	  (OR (NOT x35) (NOT x45)) (OR (NOT x35) (NOT x55))
	  (OR (NOT x35) (NOT x65)) (OR (NOT x35) (NOT x75))
	  (OR (NOT x35) (NOT x85)) (OR (NOT x35) (NOT x95))
	  (OR (NOT x45) (NOT x15)) (OR (NOT x45) (NOT x25))
	  (OR (NOT x45) (NOT x35)) (OR (NOT x45) (NOT x55))
	  (OR (NOT x45) (NOT x65)) (OR (NOT x45) (NOT x75))
	  (OR (NOT x45) (NOT x85)) (OR (NOT x45) (NOT x95))
	  (OR (NOT x55) (NOT x15)) (OR (NOT x55) (NOT x25))
	  (OR (NOT x55) (NOT x35)) (OR (NOT x55) (NOT x45))
	  (OR (NOT x55) (NOT x65)) (OR (NOT x55) (NOT x75))
	  (OR (NOT x55) (NOT x85)) (OR (NOT x55) (NOT x95))
	  (OR (NOT x65) (NOT x15)) (OR (NOT x65) (NOT x25))
	  (OR (NOT x65) (NOT x35)) (OR (NOT x65) (NOT x45))
	  (OR (NOT x65) (NOT x55)) (OR (NOT x65) (NOT x75))
	  (OR (NOT x65) (NOT x85)) (OR (NOT x65) (NOT x95))
	  (OR (NOT x75) (NOT x15)) (OR (NOT x75) (NOT x25))
	  (OR (NOT x75) (NOT x35)) (OR (NOT x75) (NOT x45))
	  (OR (NOT x75) (NOT x55)) (OR (NOT x75) (NOT x65))
	  (OR (NOT x75) (NOT x85)) (OR (NOT x75) (NOT x95))
	  (OR (NOT x85) (NOT x15)) (OR (NOT x85) (NOT x25))
	  (OR (NOT x85) (NOT x35)) (OR (NOT x85) (NOT x45))
	  (OR (NOT x85) (NOT x55)) (OR (NOT x85) (NOT x65))
	  (OR (NOT x85) (NOT x75)) (OR (NOT x85) (NOT x95))
	  (OR (NOT x95) (NOT x15)) (OR (NOT x95) (NOT x25))
	  (OR (NOT x95) (NOT x35)) (OR (NOT x95) (NOT x45))
	  (OR (NOT x95) (NOT x55)) (OR (NOT x95) (NOT x65))
	  (OR (NOT x95) (NOT x75)) (OR (NOT x95) (NOT x85))
	  (OR (NOT x16) (NOT x26)) (OR (NOT x16) (NOT x36))
	  (OR (NOT x16) (NOT x46)) (OR (NOT x16) (NOT x56))
	  (OR (NOT x16) (NOT x66)) (OR (NOT x16) (NOT x76))
	  (OR (NOT x16) (NOT x86)) (OR (NOT x16) (NOT x96))
	  (OR (NOT x26) (NOT x16)) (OR (NOT x26) (NOT x36))
	  (OR (NOT x26) (NOT x46)) (OR (NOT x26) (NOT x56))
	  (OR (NOT x26) (NOT x66)) (OR (NOT x26) (NOT x76))
	  (OR (NOT x26) (NOT x86)) (OR (NOT x26) (NOT x96))
	  (OR (NOT x36) (NOT x16)) (OR (NOT x36) (NOT x26))
	  (OR (NOT x36) (NOT x46)) (OR (NOT x36) (NOT x56))
	  (OR (NOT x36) (NOT x66)) (OR (NOT x36) (NOT x76))
	  (OR (NOT x36) (NOT x86)) (OR (NOT x36) (NOT x96))
	  (OR (NOT x46) (NOT x16)) (OR (NOT x46) (NOT x26))
	  (OR (NOT x46) (NOT x36)) (OR (NOT x46) (NOT x56))
	  (OR (NOT x46) (NOT x66)) (OR (NOT x46) (NOT x76))
	  (OR (NOT x46) (NOT x86)) (OR (NOT x46) (NOT x96))
	  (OR (NOT x56) (NOT x16)) (OR (NOT x56) (NOT x26))
	  (OR (NOT x56) (NOT x36)) (OR (NOT x56) (NOT x46))
	  (OR (NOT x56) (NOT x66)) (OR (NOT x56) (NOT x76))
	  (OR (NOT x56) (NOT x86)) (OR (NOT x56) (NOT x96))
	  (OR (NOT x66) (NOT x16)) (OR (NOT x66) (NOT x26))
	  (OR (NOT x66) (NOT x36)) (OR (NOT x66) (NOT x46))
	  (OR (NOT x66) (NOT x56)) (OR (NOT x66) (NOT x76))
	  (OR (NOT x66) (NOT x86)) (OR (NOT x66) (NOT x96))
	  (OR (NOT x76) (NOT x16)) (OR (NOT x76) (NOT x26))
	  (OR (NOT x76) (NOT x36)) (OR (NOT x76) (NOT x46))
	  (OR (NOT x76) (NOT x56)) (OR (NOT x76) (NOT x66))
	  (OR (NOT x76) (NOT x86)) (OR (NOT x76) (NOT x96))
	  (OR (NOT x86) (NOT x16)) (OR (NOT x86) (NOT x26))
	  (OR (NOT x86) (NOT x36)) (OR (NOT x86) (NOT x46))
	  (OR (NOT x86) (NOT x56)) (OR (NOT x86) (NOT x66))
	  (OR (NOT x86) (NOT x76)) (OR (NOT x86) (NOT x96))
	  (OR (NOT x96) (NOT x16)) (OR (NOT x96) (NOT x26))
	  (OR (NOT x96) (NOT x36)) (OR (NOT x96) (NOT x46))
	  (OR (NOT x96) (NOT x56)) (OR (NOT x96) (NOT x66))
	  (OR (NOT x96) (NOT x76)) (OR (NOT x96) (NOT x86))
	  (OR (NOT x17) (NOT x27)) (OR (NOT x17) (NOT x37))
	  (OR (NOT x17) (NOT x47)) (OR (NOT x17) (NOT x57))
	  (OR (NOT x17) (NOT x67)) (OR (NOT x17) (NOT x77))
	  (OR (NOT x17) (NOT x87)) (OR (NOT x17) (NOT x97))
	  (OR (NOT x27) (NOT x17)) (OR (NOT x27) (NOT x37))
	  (OR (NOT x27) (NOT x47)) (OR (NOT x27) (NOT x57))
	  (OR (NOT x27) (NOT x67)) (OR (NOT x27) (NOT x77))
	  (OR (NOT x27) (NOT x87)) (OR (NOT x27) (NOT x97))
	  (OR (NOT x37) (NOT x17)) (OR (NOT x37) (NOT x27))
	  (OR (NOT x37) (NOT x47)) (OR (NOT x37) (NOT x57))
	  (OR (NOT x37) (NOT x67)) (OR (NOT x37) (NOT x77))
	  (OR (NOT x37) (NOT x87)) (OR (NOT x37) (NOT x97))
	  (OR (NOT x47) (NOT x17)) (OR (NOT x47) (NOT x27))
	  (OR (NOT x47) (NOT x37)) (OR (NOT x47) (NOT x57))
	  (OR (NOT x47) (NOT x67)) (OR (NOT x47) (NOT x77))
	  (OR (NOT x47) (NOT x87)) (OR (NOT x47) (NOT x97))
	  (OR (NOT x57) (NOT x17)) (OR (NOT x57) (NOT x27))
	  (OR (NOT x57) (NOT x37)) (OR (NOT x57) (NOT x47))
	  (OR (NOT x57) (NOT x67)) (OR (NOT x57) (NOT x77))
	  (OR (NOT x57) (NOT x87)) (OR (NOT x57) (NOT x97))
	  (OR (NOT x67) (NOT x17)) (OR (NOT x67) (NOT x27))
	  (OR (NOT x67) (NOT x37)) (OR (NOT x67) (NOT x47))
	  (OR (NOT x67) (NOT x57)) (OR (NOT x67) (NOT x77))
	  (OR (NOT x67) (NOT x87)) (OR (NOT x67) (NOT x97))
	  (OR (NOT x77) (NOT x17)) (OR (NOT x77) (NOT x27))
	  (OR (NOT x77) (NOT x37)) (OR (NOT x77) (NOT x47))
	  (OR (NOT x77) (NOT x57)) (OR (NOT x77) (NOT x67))
	  (OR (NOT x77) (NOT x87)) (OR (NOT x77) (NOT x97))
	  (OR (NOT x87) (NOT x17)) (OR (NOT x87) (NOT x27))
	  (OR (NOT x87) (NOT x37)) (OR (NOT x87) (NOT x47))
	  (OR (NOT x87) (NOT x57)) (OR (NOT x87) (NOT x67))
	  (OR (NOT x87) (NOT x77)) (OR (NOT x87) (NOT x97))
	  (OR (NOT x97) (NOT x17)) (OR (NOT x97) (NOT x27))
	  (OR (NOT x97) (NOT x37)) (OR (NOT x97) (NOT x47))
	  (OR (NOT x97) (NOT x57)) (OR (NOT x97) (NOT x67))
	  (OR (NOT x97) (NOT x77)) (OR (NOT x97) (NOT x87))
	  (OR (NOT x18) (NOT x28)) (OR (NOT x18) (NOT x38))
	  (OR (NOT x18) (NOT x48)) (OR (NOT x18) (NOT x58))
	  (OR (NOT x18) (NOT x68)) (OR (NOT x18) (NOT x78))
	  (OR (NOT x18) (NOT x88)) (OR (NOT x18) (NOT x98))
	  (OR (NOT x28) (NOT x18)) (OR (NOT x28) (NOT x38))
	  (OR (NOT x28) (NOT x48)) (OR (NOT x28) (NOT x58))
	  (OR (NOT x28) (NOT x68)) (OR (NOT x28) (NOT x78))
	  (OR (NOT x28) (NOT x88)) (OR (NOT x28) (NOT x98))
	  (OR (NOT x38) (NOT x18)) (OR (NOT x38) (NOT x28))
	  (OR (NOT x38) (NOT x48)) (OR (NOT x38) (NOT x58))
	  (OR (NOT x38) (NOT x68)) (OR (NOT x38) (NOT x78))
	  (OR (NOT x38) (NOT x88)) (OR (NOT x38) (NOT x98))
	  (OR (NOT x48) (NOT x18)) (OR (NOT x48) (NOT x28))
	  (OR (NOT x48) (NOT x38)) (OR (NOT x48) (NOT x58))
	  (OR (NOT x48) (NOT x68)) (OR (NOT x48) (NOT x78))
	  (OR (NOT x48) (NOT x88)) (OR (NOT x48) (NOT x98))
	  (OR (NOT x58) (NOT x18)) (OR (NOT x58) (NOT x28))
	  (OR (NOT x58) (NOT x38)) (OR (NOT x58) (NOT x48))
	  (OR (NOT x58) (NOT x68)) (OR (NOT x58) (NOT x78))
	  (OR (NOT x58) (NOT x88)) (OR (NOT x58) (NOT x98))
	  (OR (NOT x68) (NOT x18)) (OR (NOT x68) (NOT x28))
	  (OR (NOT x68) (NOT x38)) (OR (NOT x68) (NOT x48))
	  (OR (NOT x68) (NOT x58)) (OR (NOT x68) (NOT x78))
	  (OR (NOT x68) (NOT x88)) (OR (NOT x68) (NOT x98))
	  (OR (NOT x78) (NOT x18)) (OR (NOT x78) (NOT x28))
	  (OR (NOT x78) (NOT x38)) (OR (NOT x78) (NOT x48))
	  (OR (NOT x78) (NOT x58)) (OR (NOT x78) (NOT x68))
	  (OR (NOT x78) (NOT x88)) (OR (NOT x78) (NOT x98))
	  (OR (NOT x88) (NOT x18)) (OR (NOT x88) (NOT x28))
	  (OR (NOT x88) (NOT x38)) (OR (NOT x88) (NOT x48))
	  (OR (NOT x88) (NOT x58)) (OR (NOT x88) (NOT x68))
	  (OR (NOT x88) (NOT x78)) (OR (NOT x88) (NOT x98))
	  (OR (NOT x98) (NOT x18)) (OR (NOT x98) (NOT x28))
	  (OR (NOT x98) (NOT x38)) (OR (NOT x98) (NOT x48))
	  (OR (NOT x98) (NOT x58)) (OR (NOT x98) (NOT x68))
	  (OR (NOT x98) (NOT x78)) (OR (NOT x98) (NOT x88))
	  (OR (NOT x19) (NOT x29)) (OR (NOT x19) (NOT x39))
	  (OR (NOT x19) (NOT x49)) (OR (NOT x19) (NOT x59))
	  (OR (NOT x19) (NOT x69)) (OR (NOT x19) (NOT x79))
	  (OR (NOT x19) (NOT x89)) (OR (NOT x19) (NOT x99))
	  (OR (NOT x29) (NOT x19)) (OR (NOT x29) (NOT x39))
	  (OR (NOT x29) (NOT x49)) (OR (NOT x29) (NOT x59))
	  (OR (NOT x29) (NOT x69)) (OR (NOT x29) (NOT x79))
	  (OR (NOT x29) (NOT x89)) (OR (NOT x29) (NOT x99))
	  (OR (NOT x39) (NOT x19)) (OR (NOT x39) (NOT x29))
	  (OR (NOT x39) (NOT x49)) (OR (NOT x39) (NOT x59))
	  (OR (NOT x39) (NOT x69)) (OR (NOT x39) (NOT x79))
	  (OR (NOT x39) (NOT x89)) (OR (NOT x39) (NOT x99))
	  (OR (NOT x49) (NOT x19)) (OR (NOT x49) (NOT x29))
	  (OR (NOT x49) (NOT x39)) (OR (NOT x49) (NOT x59))
	  (OR (NOT x49) (NOT x69)) (OR (NOT x49) (NOT x79))
	  (OR (NOT x49) (NOT x89)) (OR (NOT x49) (NOT x99))
	  (OR (NOT x59) (NOT x19)) (OR (NOT x59) (NOT x29))
	  (OR (NOT x59) (NOT x39)) (OR (NOT x59) (NOT x49))
	  (OR (NOT x59) (NOT x69)) (OR (NOT x59) (NOT x79))
	  (OR (NOT x59) (NOT x89)) (OR (NOT x59) (NOT x99))
	  (OR (NOT x69) (NOT x19)) (OR (NOT x69) (NOT x29))
	  (OR (NOT x69) (NOT x39)) (OR (NOT x69) (NOT x49))
	  (OR (NOT x69) (NOT x59)) (OR (NOT x69) (NOT x79))
	  (OR (NOT x69) (NOT x89)) (OR (NOT x69) (NOT x99))
	  (OR (NOT x79) (NOT x19)) (OR (NOT x79) (NOT x29))
	  (OR (NOT x79) (NOT x39)) (OR (NOT x79) (NOT x49))
	  (OR (NOT x79) (NOT x59)) (OR (NOT x79) (NOT x69))
	  (OR (NOT x79) (NOT x89)) (OR (NOT x79) (NOT x99))
	  (OR (NOT x89) (NOT x19)) (OR (NOT x89) (NOT x29))
	  (OR (NOT x89) (NOT x39)) (OR (NOT x89) (NOT x49))
	  (OR (NOT x89) (NOT x59)) (OR (NOT x89) (NOT x69))
	  (OR (NOT x89) (NOT x79)) (OR (NOT x89) (NOT x99))
	  (OR (NOT x99) (NOT x19)) (OR (NOT x99) (NOT x29))
	  (OR (NOT x99) (NOT x39)) (OR (NOT x99) (NOT x49))
	  (OR (NOT x99) (NOT x59)) (OR (NOT x99) (NOT x69))
	  (OR (NOT x99) (NOT x79)) (OR (NOT x99) (NOT x89))))


;; axiom 4

;; (let ((V (list 1 2 3 4 5 6 7 8 9)))
;;   (loop for i in V
;; 	append (loop for j in V
;; 		     append (loop for k in V
;; 				  when (not (equal k j))
;; 				    collect `(or
;; 					      (not ,(format nil "x~a~a" i j))
;; 					      (not ,(format nil "x~a~a" i k)))))))

(assert '(and
	  (OR (NOT x11) (NOT x12)) (OR (NOT x11) (NOT x13))
	  (OR (NOT x11) (NOT x14)) (OR (NOT x11) (NOT x15))
	  (OR (NOT x11) (NOT x16)) (OR (NOT x11) (NOT x17))
	  (OR (NOT x11) (NOT x18)) (OR (NOT x11) (NOT x19))
	  (OR (NOT x12) (NOT x11)) (OR (NOT x12) (NOT x13))
	  (OR (NOT x12) (NOT x14)) (OR (NOT x12) (NOT x15))
	  (OR (NOT x12) (NOT x16)) (OR (NOT x12) (NOT x17))
	  (OR (NOT x12) (NOT x18)) (OR (NOT x12) (NOT x19))
	  (OR (NOT x13) (NOT x11)) (OR (NOT x13) (NOT x12))
	  (OR (NOT x13) (NOT x14)) (OR (NOT x13) (NOT x15))
	  (OR (NOT x13) (NOT x16)) (OR (NOT x13) (NOT x17))
	  (OR (NOT x13) (NOT x18)) (OR (NOT x13) (NOT x19))
	  (OR (NOT x14) (NOT x11)) (OR (NOT x14) (NOT x12))
	  (OR (NOT x14) (NOT x13)) (OR (NOT x14) (NOT x15))
	  (OR (NOT x14) (NOT x16)) (OR (NOT x14) (NOT x17))
	  (OR (NOT x14) (NOT x18)) (OR (NOT x14) (NOT x19))
	  (OR (NOT x15) (NOT x11)) (OR (NOT x15) (NOT x12))
	  (OR (NOT x15) (NOT x13)) (OR (NOT x15) (NOT x14))
	  (OR (NOT x15) (NOT x16)) (OR (NOT x15) (NOT x17))
	  (OR (NOT x15) (NOT x18)) (OR (NOT x15) (NOT x19))
	  (OR (NOT x16) (NOT x11)) (OR (NOT x16) (NOT x12))
	  (OR (NOT x16) (NOT x13)) (OR (NOT x16) (NOT x14))
	  (OR (NOT x16) (NOT x15)) (OR (NOT x16) (NOT x17))
	  (OR (NOT x16) (NOT x18)) (OR (NOT x16) (NOT x19))
	  (OR (NOT x17) (NOT x11)) (OR (NOT x17) (NOT x12))
	  (OR (NOT x17) (NOT x13)) (OR (NOT x17) (NOT x14))
	  (OR (NOT x17) (NOT x15)) (OR (NOT x17) (NOT x16))
	  (OR (NOT x17) (NOT x18)) (OR (NOT x17) (NOT x19))
	  (OR (NOT x18) (NOT x11)) (OR (NOT x18) (NOT x12))
	  (OR (NOT x18) (NOT x13)) (OR (NOT x18) (NOT x14))
	  (OR (NOT x18) (NOT x15)) (OR (NOT x18) (NOT x16))
	  (OR (NOT x18) (NOT x17)) (OR (NOT x18) (NOT x19))
	  (OR (NOT x19) (NOT x11)) (OR (NOT x19) (NOT x12))
	  (OR (NOT x19) (NOT x13)) (OR (NOT x19) (NOT x14))
	  (OR (NOT x19) (NOT x15)) (OR (NOT x19) (NOT x16))
	  (OR (NOT x19) (NOT x17)) (OR (NOT x19) (NOT x18))
	  (OR (NOT x21) (NOT x22)) (OR (NOT x21) (NOT x23))
	  (OR (NOT x21) (NOT x24)) (OR (NOT x21) (NOT x25))
	  (OR (NOT x21) (NOT x26)) (OR (NOT x21) (NOT x27))
	  (OR (NOT x21) (NOT x28)) (OR (NOT x21) (NOT x29))
	  (OR (NOT x22) (NOT x21)) (OR (NOT x22) (NOT x23))
	  (OR (NOT x22) (NOT x24)) (OR (NOT x22) (NOT x25))
	  (OR (NOT x22) (NOT x26)) (OR (NOT x22) (NOT x27))
	  (OR (NOT x22) (NOT x28)) (OR (NOT x22) (NOT x29))
	  (OR (NOT x23) (NOT x21)) (OR (NOT x23) (NOT x22))
	  (OR (NOT x23) (NOT x24)) (OR (NOT x23) (NOT x25))
	  (OR (NOT x23) (NOT x26)) (OR (NOT x23) (NOT x27))
	  (OR (NOT x23) (NOT x28)) (OR (NOT x23) (NOT x29))
	  (OR (NOT x24) (NOT x21)) (OR (NOT x24) (NOT x22))
	  (OR (NOT x24) (NOT x23)) (OR (NOT x24) (NOT x25))
	  (OR (NOT x24) (NOT x26)) (OR (NOT x24) (NOT x27))
	  (OR (NOT x24) (NOT x28)) (OR (NOT x24) (NOT x29))
	  (OR (NOT x25) (NOT x21)) (OR (NOT x25) (NOT x22))
	  (OR (NOT x25) (NOT x23)) (OR (NOT x25) (NOT x24))
	  (OR (NOT x25) (NOT x26)) (OR (NOT x25) (NOT x27))
	  (OR (NOT x25) (NOT x28)) (OR (NOT x25) (NOT x29))
	  (OR (NOT x26) (NOT x21)) (OR (NOT x26) (NOT x22))
	  (OR (NOT x26) (NOT x23)) (OR (NOT x26) (NOT x24))
	  (OR (NOT x26) (NOT x25)) (OR (NOT x26) (NOT x27))
	  (OR (NOT x26) (NOT x28)) (OR (NOT x26) (NOT x29))
	  (OR (NOT x27) (NOT x21)) (OR (NOT x27) (NOT x22))
	  (OR (NOT x27) (NOT x23)) (OR (NOT x27) (NOT x24))
	  (OR (NOT x27) (NOT x25)) (OR (NOT x27) (NOT x26))
	  (OR (NOT x27) (NOT x28)) (OR (NOT x27) (NOT x29))
	  (OR (NOT x28) (NOT x21)) (OR (NOT x28) (NOT x22))
	  (OR (NOT x28) (NOT x23)) (OR (NOT x28) (NOT x24))
	  (OR (NOT x28) (NOT x25)) (OR (NOT x28) (NOT x26))
	  (OR (NOT x28) (NOT x27)) (OR (NOT x28) (NOT x29))
	  (OR (NOT x29) (NOT x21)) (OR (NOT x29) (NOT x22))
	  (OR (NOT x29) (NOT x23)) (OR (NOT x29) (NOT x24))
	  (OR (NOT x29) (NOT x25)) (OR (NOT x29) (NOT x26))
	  (OR (NOT x29) (NOT x27)) (OR (NOT x29) (NOT x28))
	  (OR (NOT x31) (NOT x32)) (OR (NOT x31) (NOT x33))
	  (OR (NOT x31) (NOT x34)) (OR (NOT x31) (NOT x35))
	  (OR (NOT x31) (NOT x36)) (OR (NOT x31) (NOT x37))
	  (OR (NOT x31) (NOT x38)) (OR (NOT x31) (NOT x39))
	  (OR (NOT x32) (NOT x31)) (OR (NOT x32) (NOT x33))
	  (OR (NOT x32) (NOT x34)) (OR (NOT x32) (NOT x35))
	  (OR (NOT x32) (NOT x36)) (OR (NOT x32) (NOT x37))
	  (OR (NOT x32) (NOT x38)) (OR (NOT x32) (NOT x39))
	  (OR (NOT x33) (NOT x31)) (OR (NOT x33) (NOT x32))
	  (OR (NOT x33) (NOT x34)) (OR (NOT x33) (NOT x35))
	  (OR (NOT x33) (NOT x36)) (OR (NOT x33) (NOT x37))
	  (OR (NOT x33) (NOT x38)) (OR (NOT x33) (NOT x39))
	  (OR (NOT x34) (NOT x31)) (OR (NOT x34) (NOT x32))
	  (OR (NOT x34) (NOT x33)) (OR (NOT x34) (NOT x35))
	  (OR (NOT x34) (NOT x36)) (OR (NOT x34) (NOT x37))
	  (OR (NOT x34) (NOT x38)) (OR (NOT x34) (NOT x39))
	  (OR (NOT x35) (NOT x31)) (OR (NOT x35) (NOT x32))
	  (OR (NOT x35) (NOT x33)) (OR (NOT x35) (NOT x34))
	  (OR (NOT x35) (NOT x36)) (OR (NOT x35) (NOT x37))
	  (OR (NOT x35) (NOT x38)) (OR (NOT x35) (NOT x39))
	  (OR (NOT x36) (NOT x31)) (OR (NOT x36) (NOT x32))
	  (OR (NOT x36) (NOT x33)) (OR (NOT x36) (NOT x34))
	  (OR (NOT x36) (NOT x35)) (OR (NOT x36) (NOT x37))
	  (OR (NOT x36) (NOT x38)) (OR (NOT x36) (NOT x39))
	  (OR (NOT x37) (NOT x31)) (OR (NOT x37) (NOT x32))
	  (OR (NOT x37) (NOT x33)) (OR (NOT x37) (NOT x34))
	  (OR (NOT x37) (NOT x35)) (OR (NOT x37) (NOT x36))
	  (OR (NOT x37) (NOT x38)) (OR (NOT x37) (NOT x39))
	  (OR (NOT x38) (NOT x31)) (OR (NOT x38) (NOT x32))
	  (OR (NOT x38) (NOT x33)) (OR (NOT x38) (NOT x34))
	  (OR (NOT x38) (NOT x35)) (OR (NOT x38) (NOT x36))
	  (OR (NOT x38) (NOT x37)) (OR (NOT x38) (NOT x39))
	  (OR (NOT x39) (NOT x31)) (OR (NOT x39) (NOT x32))
	  (OR (NOT x39) (NOT x33)) (OR (NOT x39) (NOT x34))
	  (OR (NOT x39) (NOT x35)) (OR (NOT x39) (NOT x36))
	  (OR (NOT x39) (NOT x37)) (OR (NOT x39) (NOT x38))
	  (OR (NOT x41) (NOT x42)) (OR (NOT x41) (NOT x43))
	  (OR (NOT x41) (NOT x44)) (OR (NOT x41) (NOT x45))
	  (OR (NOT x41) (NOT x46)) (OR (NOT x41) (NOT x47))
	  (OR (NOT x41) (NOT x48)) (OR (NOT x41) (NOT x49))
	  (OR (NOT x42) (NOT x41)) (OR (NOT x42) (NOT x43))
	  (OR (NOT x42) (NOT x44)) (OR (NOT x42) (NOT x45))
	  (OR (NOT x42) (NOT x46)) (OR (NOT x42) (NOT x47))
	  (OR (NOT x42) (NOT x48)) (OR (NOT x42) (NOT x49))
	  (OR (NOT x43) (NOT x41)) (OR (NOT x43) (NOT x42))
	  (OR (NOT x43) (NOT x44)) (OR (NOT x43) (NOT x45))
	  (OR (NOT x43) (NOT x46)) (OR (NOT x43) (NOT x47))
	  (OR (NOT x43) (NOT x48)) (OR (NOT x43) (NOT x49))
	  (OR (NOT x44) (NOT x41)) (OR (NOT x44) (NOT x42))
	  (OR (NOT x44) (NOT x43)) (OR (NOT x44) (NOT x45))
	  (OR (NOT x44) (NOT x46)) (OR (NOT x44) (NOT x47))
	  (OR (NOT x44) (NOT x48)) (OR (NOT x44) (NOT x49))
	  (OR (NOT x45) (NOT x41)) (OR (NOT x45) (NOT x42))
	  (OR (NOT x45) (NOT x43)) (OR (NOT x45) (NOT x44))
	  (OR (NOT x45) (NOT x46)) (OR (NOT x45) (NOT x47))
	  (OR (NOT x45) (NOT x48)) (OR (NOT x45) (NOT x49))
	  (OR (NOT x46) (NOT x41)) (OR (NOT x46) (NOT x42))
	  (OR (NOT x46) (NOT x43)) (OR (NOT x46) (NOT x44))
	  (OR (NOT x46) (NOT x45)) (OR (NOT x46) (NOT x47))
	  (OR (NOT x46) (NOT x48)) (OR (NOT x46) (NOT x49))
	  (OR (NOT x47) (NOT x41)) (OR (NOT x47) (NOT x42))
	  (OR (NOT x47) (NOT x43)) (OR (NOT x47) (NOT x44))
	  (OR (NOT x47) (NOT x45)) (OR (NOT x47) (NOT x46))
	  (OR (NOT x47) (NOT x48)) (OR (NOT x47) (NOT x49))
	  (OR (NOT x48) (NOT x41)) (OR (NOT x48) (NOT x42))
	  (OR (NOT x48) (NOT x43)) (OR (NOT x48) (NOT x44))
	  (OR (NOT x48) (NOT x45)) (OR (NOT x48) (NOT x46))
	  (OR (NOT x48) (NOT x47)) (OR (NOT x48) (NOT x49))
	  (OR (NOT x49) (NOT x41)) (OR (NOT x49) (NOT x42))
	  (OR (NOT x49) (NOT x43)) (OR (NOT x49) (NOT x44))
	  (OR (NOT x49) (NOT x45)) (OR (NOT x49) (NOT x46))
	  (OR (NOT x49) (NOT x47)) (OR (NOT x49) (NOT x48))
	  (OR (NOT x51) (NOT x52)) (OR (NOT x51) (NOT x53))
	  (OR (NOT x51) (NOT x54)) (OR (NOT x51) (NOT x55))
	  (OR (NOT x51) (NOT x56)) (OR (NOT x51) (NOT x57))
	  (OR (NOT x51) (NOT x58)) (OR (NOT x51) (NOT x59))
	  (OR (NOT x52) (NOT x51)) (OR (NOT x52) (NOT x53))
	  (OR (NOT x52) (NOT x54)) (OR (NOT x52) (NOT x55))
	  (OR (NOT x52) (NOT x56)) (OR (NOT x52) (NOT x57))
	  (OR (NOT x52) (NOT x58)) (OR (NOT x52) (NOT x59))
	  (OR (NOT x53) (NOT x51)) (OR (NOT x53) (NOT x52))
	  (OR (NOT x53) (NOT x54)) (OR (NOT x53) (NOT x55))
	  (OR (NOT x53) (NOT x56)) (OR (NOT x53) (NOT x57))
	  (OR (NOT x53) (NOT x58)) (OR (NOT x53) (NOT x59))
	  (OR (NOT x54) (NOT x51)) (OR (NOT x54) (NOT x52))
	  (OR (NOT x54) (NOT x53)) (OR (NOT x54) (NOT x55))
	  (OR (NOT x54) (NOT x56)) (OR (NOT x54) (NOT x57))
	  (OR (NOT x54) (NOT x58)) (OR (NOT x54) (NOT x59))
	  (OR (NOT x55) (NOT x51)) (OR (NOT x55) (NOT x52))
	  (OR (NOT x55) (NOT x53)) (OR (NOT x55) (NOT x54))
	  (OR (NOT x55) (NOT x56)) (OR (NOT x55) (NOT x57))
	  (OR (NOT x55) (NOT x58)) (OR (NOT x55) (NOT x59))
	  (OR (NOT x56) (NOT x51)) (OR (NOT x56) (NOT x52))
	  (OR (NOT x56) (NOT x53)) (OR (NOT x56) (NOT x54))
	  (OR (NOT x56) (NOT x55)) (OR (NOT x56) (NOT x57))
	  (OR (NOT x56) (NOT x58)) (OR (NOT x56) (NOT x59))
	  (OR (NOT x57) (NOT x51)) (OR (NOT x57) (NOT x52))
	  (OR (NOT x57) (NOT x53)) (OR (NOT x57) (NOT x54))
	  (OR (NOT x57) (NOT x55)) (OR (NOT x57) (NOT x56))
	  (OR (NOT x57) (NOT x58)) (OR (NOT x57) (NOT x59))
	  (OR (NOT x58) (NOT x51)) (OR (NOT x58) (NOT x52))
	  (OR (NOT x58) (NOT x53)) (OR (NOT x58) (NOT x54))
	  (OR (NOT x58) (NOT x55)) (OR (NOT x58) (NOT x56))
	  (OR (NOT x58) (NOT x57)) (OR (NOT x58) (NOT x59))
	  (OR (NOT x59) (NOT x51)) (OR (NOT x59) (NOT x52))
	  (OR (NOT x59) (NOT x53)) (OR (NOT x59) (NOT x54))
	  (OR (NOT x59) (NOT x55)) (OR (NOT x59) (NOT x56))
	  (OR (NOT x59) (NOT x57)) (OR (NOT x59) (NOT x58))
	  (OR (NOT x61) (NOT x62)) (OR (NOT x61) (NOT x63))
	  (OR (NOT x61) (NOT x64)) (OR (NOT x61) (NOT x65))
	  (OR (NOT x61) (NOT x66)) (OR (NOT x61) (NOT x67))
	  (OR (NOT x61) (NOT x68)) (OR (NOT x61) (NOT x69))
	  (OR (NOT x62) (NOT x61)) (OR (NOT x62) (NOT x63))
	  (OR (NOT x62) (NOT x64)) (OR (NOT x62) (NOT x65))
	  (OR (NOT x62) (NOT x66)) (OR (NOT x62) (NOT x67))
	  (OR (NOT x62) (NOT x68)) (OR (NOT x62) (NOT x69))
	  (OR (NOT x63) (NOT x61)) (OR (NOT x63) (NOT x62))
	  (OR (NOT x63) (NOT x64)) (OR (NOT x63) (NOT x65))
	  (OR (NOT x63) (NOT x66)) (OR (NOT x63) (NOT x67))
	  (OR (NOT x63) (NOT x68)) (OR (NOT x63) (NOT x69))
	  (OR (NOT x64) (NOT x61)) (OR (NOT x64) (NOT x62))
	  (OR (NOT x64) (NOT x63)) (OR (NOT x64) (NOT x65))
	  (OR (NOT x64) (NOT x66)) (OR (NOT x64) (NOT x67))
	  (OR (NOT x64) (NOT x68)) (OR (NOT x64) (NOT x69))
	  (OR (NOT x65) (NOT x61)) (OR (NOT x65) (NOT x62))
	  (OR (NOT x65) (NOT x63)) (OR (NOT x65) (NOT x64))
	  (OR (NOT x65) (NOT x66)) (OR (NOT x65) (NOT x67))
	  (OR (NOT x65) (NOT x68)) (OR (NOT x65) (NOT x69))
	  (OR (NOT x66) (NOT x61)) (OR (NOT x66) (NOT x62))
	  (OR (NOT x66) (NOT x63)) (OR (NOT x66) (NOT x64))
	  (OR (NOT x66) (NOT x65)) (OR (NOT x66) (NOT x67))
	  (OR (NOT x66) (NOT x68)) (OR (NOT x66) (NOT x69))
	  (OR (NOT x67) (NOT x61)) (OR (NOT x67) (NOT x62))
	  (OR (NOT x67) (NOT x63)) (OR (NOT x67) (NOT x64))
	  (OR (NOT x67) (NOT x65)) (OR (NOT x67) (NOT x66))
	  (OR (NOT x67) (NOT x68)) (OR (NOT x67) (NOT x69))
	  (OR (NOT x68) (NOT x61)) (OR (NOT x68) (NOT x62))
	  (OR (NOT x68) (NOT x63)) (OR (NOT x68) (NOT x64))
	  (OR (NOT x68) (NOT x65)) (OR (NOT x68) (NOT x66))
	  (OR (NOT x68) (NOT x67)) (OR (NOT x68) (NOT x69))
	  (OR (NOT x69) (NOT x61)) (OR (NOT x69) (NOT x62))
	  (OR (NOT x69) (NOT x63)) (OR (NOT x69) (NOT x64))
	  (OR (NOT x69) (NOT x65)) (OR (NOT x69) (NOT x66))
	  (OR (NOT x69) (NOT x67)) (OR (NOT x69) (NOT x68))
	  (OR (NOT x71) (NOT x72)) (OR (NOT x71) (NOT x73))
	  (OR (NOT x71) (NOT x74)) (OR (NOT x71) (NOT x75))
	  (OR (NOT x71) (NOT x76)) (OR (NOT x71) (NOT x77))
	  (OR (NOT x71) (NOT x78)) (OR (NOT x71) (NOT x79))
	  (OR (NOT x72) (NOT x71)) (OR (NOT x72) (NOT x73))
	  (OR (NOT x72) (NOT x74)) (OR (NOT x72) (NOT x75))
	  (OR (NOT x72) (NOT x76)) (OR (NOT x72) (NOT x77))
	  (OR (NOT x72) (NOT x78)) (OR (NOT x72) (NOT x79))
	  (OR (NOT x73) (NOT x71)) (OR (NOT x73) (NOT x72))
	  (OR (NOT x73) (NOT x74)) (OR (NOT x73) (NOT x75))
	  (OR (NOT x73) (NOT x76)) (OR (NOT x73) (NOT x77))
	  (OR (NOT x73) (NOT x78)) (OR (NOT x73) (NOT x79))
	  (OR (NOT x74) (NOT x71)) (OR (NOT x74) (NOT x72))
	  (OR (NOT x74) (NOT x73)) (OR (NOT x74) (NOT x75))
	  (OR (NOT x74) (NOT x76)) (OR (NOT x74) (NOT x77))
	  (OR (NOT x74) (NOT x78)) (OR (NOT x74) (NOT x79))
	  (OR (NOT x75) (NOT x71)) (OR (NOT x75) (NOT x72))
	  (OR (NOT x75) (NOT x73)) (OR (NOT x75) (NOT x74))
	  (OR (NOT x75) (NOT x76)) (OR (NOT x75) (NOT x77))
	  (OR (NOT x75) (NOT x78)) (OR (NOT x75) (NOT x79))
	  (OR (NOT x76) (NOT x71)) (OR (NOT x76) (NOT x72))
	  (OR (NOT x76) (NOT x73)) (OR (NOT x76) (NOT x74))
	  (OR (NOT x76) (NOT x75)) (OR (NOT x76) (NOT x77))
	  (OR (NOT x76) (NOT x78)) (OR (NOT x76) (NOT x79))
	  (OR (NOT x77) (NOT x71)) (OR (NOT x77) (NOT x72))
	  (OR (NOT x77) (NOT x73)) (OR (NOT x77) (NOT x74))
	  (OR (NOT x77) (NOT x75)) (OR (NOT x77) (NOT x76))
	  (OR (NOT x77) (NOT x78)) (OR (NOT x77) (NOT x79))
	  (OR (NOT x78) (NOT x71)) (OR (NOT x78) (NOT x72))
	  (OR (NOT x78) (NOT x73)) (OR (NOT x78) (NOT x74))
	  (OR (NOT x78) (NOT x75)) (OR (NOT x78) (NOT x76))
	  (OR (NOT x78) (NOT x77)) (OR (NOT x78) (NOT x79))
	  (OR (NOT x79) (NOT x71)) (OR (NOT x79) (NOT x72))
	  (OR (NOT x79) (NOT x73)) (OR (NOT x79) (NOT x74))
	  (OR (NOT x79) (NOT x75)) (OR (NOT x79) (NOT x76))
	  (OR (NOT x79) (NOT x77)) (OR (NOT x79) (NOT x78))
	  (OR (NOT x81) (NOT x82)) (OR (NOT x81) (NOT x83))
	  (OR (NOT x81) (NOT x84)) (OR (NOT x81) (NOT x85))
	  (OR (NOT x81) (NOT x86)) (OR (NOT x81) (NOT x87))
	  (OR (NOT x81) (NOT x88)) (OR (NOT x81) (NOT x89))
	  (OR (NOT x82) (NOT x81)) (OR (NOT x82) (NOT x83))
	  (OR (NOT x82) (NOT x84)) (OR (NOT x82) (NOT x85))
	  (OR (NOT x82) (NOT x86)) (OR (NOT x82) (NOT x87))
	  (OR (NOT x82) (NOT x88)) (OR (NOT x82) (NOT x89))
	  (OR (NOT x83) (NOT x81)) (OR (NOT x83) (NOT x82))
	  (OR (NOT x83) (NOT x84)) (OR (NOT x83) (NOT x85))
	  (OR (NOT x83) (NOT x86)) (OR (NOT x83) (NOT x87))
	  (OR (NOT x83) (NOT x88)) (OR (NOT x83) (NOT x89))
	  (OR (NOT x84) (NOT x81)) (OR (NOT x84) (NOT x82))
	  (OR (NOT x84) (NOT x83)) (OR (NOT x84) (NOT x85))
	  (OR (NOT x84) (NOT x86)) (OR (NOT x84) (NOT x87))
	  (OR (NOT x84) (NOT x88)) (OR (NOT x84) (NOT x89))
	  (OR (NOT x85) (NOT x81)) (OR (NOT x85) (NOT x82))
	  (OR (NOT x85) (NOT x83)) (OR (NOT x85) (NOT x84))
	  (OR (NOT x85) (NOT x86)) (OR (NOT x85) (NOT x87))
	  (OR (NOT x85) (NOT x88)) (OR (NOT x85) (NOT x89))
	  (OR (NOT x86) (NOT x81)) (OR (NOT x86) (NOT x82))
	  (OR (NOT x86) (NOT x83)) (OR (NOT x86) (NOT x84))
	  (OR (NOT x86) (NOT x85)) (OR (NOT x86) (NOT x87))
	  (OR (NOT x86) (NOT x88)) (OR (NOT x86) (NOT x89))
	  (OR (NOT x87) (NOT x81)) (OR (NOT x87) (NOT x82))
	  (OR (NOT x87) (NOT x83)) (OR (NOT x87) (NOT x84))
	  (OR (NOT x87) (NOT x85)) (OR (NOT x87) (NOT x86))
	  (OR (NOT x87) (NOT x88)) (OR (NOT x87) (NOT x89))
	  (OR (NOT x88) (NOT x81)) (OR (NOT x88) (NOT x82))
	  (OR (NOT x88) (NOT x83)) (OR (NOT x88) (NOT x84))
	  (OR (NOT x88) (NOT x85)) (OR (NOT x88) (NOT x86))
	  (OR (NOT x88) (NOT x87)) (OR (NOT x88) (NOT x89))
	  (OR (NOT x89) (NOT x81)) (OR (NOT x89) (NOT x82))
	  (OR (NOT x89) (NOT x83)) (OR (NOT x89) (NOT x84))
	  (OR (NOT x89) (NOT x85)) (OR (NOT x89) (NOT x86))
	  (OR (NOT x89) (NOT x87)) (OR (NOT x89) (NOT x88))
	  (OR (NOT x91) (NOT x92)) (OR (NOT x91) (NOT x93))
	  (OR (NOT x91) (NOT x94)) (OR (NOT x91) (NOT x95))
	  (OR (NOT x91) (NOT x96)) (OR (NOT x91) (NOT x97))
	  (OR (NOT x91) (NOT x98)) (OR (NOT x91) (NOT x99))
	  (OR (NOT x92) (NOT x91)) (OR (NOT x92) (NOT x93))
	  (OR (NOT x92) (NOT x94)) (OR (NOT x92) (NOT x95))
	  (OR (NOT x92) (NOT x96)) (OR (NOT x92) (NOT x97))
	  (OR (NOT x92) (NOT x98)) (OR (NOT x92) (NOT x99))
	  (OR (NOT x93) (NOT x91)) (OR (NOT x93) (NOT x92))
	  (OR (NOT x93) (NOT x94)) (OR (NOT x93) (NOT x95))
	  (OR (NOT x93) (NOT x96)) (OR (NOT x93) (NOT x97))
	  (OR (NOT x93) (NOT x98)) (OR (NOT x93) (NOT x99))
	  (OR (NOT x94) (NOT x91)) (OR (NOT x94) (NOT x92))
	  (OR (NOT x94) (NOT x93)) (OR (NOT x94) (NOT x95))
	  (OR (NOT x94) (NOT x96)) (OR (NOT x94) (NOT x97))
	  (OR (NOT x94) (NOT x98)) (OR (NOT x94) (NOT x99))
	  (OR (NOT x95) (NOT x91)) (OR (NOT x95) (NOT x92))
	  (OR (NOT x95) (NOT x93)) (OR (NOT x95) (NOT x94))
	  (OR (NOT x95) (NOT x96)) (OR (NOT x95) (NOT x97))
	  (OR (NOT x95) (NOT x98)) (OR (NOT x95) (NOT x99))
	  (OR (NOT x96) (NOT x91)) (OR (NOT x96) (NOT x92))
	  (OR (NOT x96) (NOT x93)) (OR (NOT x96) (NOT x94))
	  (OR (NOT x96) (NOT x95)) (OR (NOT x96) (NOT x97))
	  (OR (NOT x96) (NOT x98)) (OR (NOT x96) (NOT x99))
	  (OR (NOT x97) (NOT x91)) (OR (NOT x97) (NOT x92))
	  (OR (NOT x97) (NOT x93)) (OR (NOT x97) (NOT x94))
	  (OR (NOT x97) (NOT x95)) (OR (NOT x97) (NOT x96))
	  (OR (NOT x97) (NOT x98)) (OR (NOT x97) (NOT x99))
	  (OR (NOT x98) (NOT x91)) (OR (NOT x98) (NOT x92))
	  (OR (NOT x98) (NOT x93)) (OR (NOT x98) (NOT x94))
	  (OR (NOT x98) (NOT x95)) (OR (NOT x98) (NOT x96))
	  (OR (NOT x98) (NOT x97)) (OR (NOT x98) (NOT x99))
	  (OR (NOT x99) (NOT x91)) (OR (NOT x99) (NOT x92))
	  (OR (NOT x99) (NOT x93)) (OR (NOT x99) (NOT x94))
	  (OR (NOT x99) (NOT x95)) (OR (NOT x99) (NOT x96))
	  (OR (NOT x99) (NOT x97)) (OR (NOT x99) (NOT x98))))


;; wrong ideia, its not logical consequence of the assertions
(prove '(and x12 x21 x34 x45 x53 x69 x76 x88 x97))
